/* App release version (major.minor.patch). Bump this for each release and
   tag it in git (e.g. `git tag v1.0.0`). Separate from the backup format
   version, the localStorage key versions, and the service worker cache name. */
export var APP_VERSION = "1.0.0";
